{
  /*
    TypeScript is being used to do type checking across both JavaScript and TypeScript file.
    In particular, this picks up some problems in the JSDoc in the JavaScript files, and validates the code
    is consistent with the JSDoc.
     
    The settings here are used by VSCode, and TypeScript checks are enabled in JavaScript files. 
    
    See also tsconfig.js.json and tsconfig.ts.json.
  */
  /* Visit https://aka.ms/tsconfig to read more about tsconfig configuration. */
  "compilerOptions": {
    "lib": ["es2021"],
    "module": "node16",
    "target": "es2021",

    "allowJs": true,
    "checkJs": true,
    
    /* less strict settings to match current JavaScript lib code */
    "noImplicitAny": false, 
    "noImplicitThis": false,
    "strictNullChecks": false,

    "types": [
      "node",
      "jest"
    ],
    "noEmit": true, /* just type checking and not emitting transpiled files */
    "skipLibCheck": false, /* we want to check our hand crafted definitions */
    "forceConsistentCasingInFileNames": true,
    "esModuleInterop": true /* recommended TypeScript config, and tests/ts-imports.tst.ts covers related "import * as commander" behaviour */
  },
  "include": [
    /* JavaScript. Should match includes in tsconfig.js.json. */
    "*.js",
    "*.mjs",
    "lib/**/*.js",
    /* TypeScript. Should match includes in tsconfig.ts.json. */
    "**/*.ts",
    "**/*.mts"
  ],
  "exclude": [
    "node_modules"
  ]
}
